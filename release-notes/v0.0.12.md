## v0.0.12

### 새 기능
- **덱 목록 정렬** — 덱 목록 상단 정렬 바를 탭하면 이름 순 / 오래된 순 / 최근 순으로 모드가 순환됩니다.
  - 오래된 순: 학습한 지 오래된 덱을 맨 위에 표시 (30일 이상 미학습 덱은 하단 고정)
  - 최근 순: 가장 최근에 학습한 덱을 맨 위에 표시 (30일 이상 미학습 덱은 하단 고정)
  - 중첩 덱은 자식 중 가장 최근 학습 시각을 기준으로 부모 순서를 결정
  - 정렬 설정은 앱 재시작 후에도 유지됩니다
